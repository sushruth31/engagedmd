{
  "name": "credit-card-validator",
  "version": "1.0.0",
  "description": "Full-stack credit card validator using the Luhn checksum algorithm.",
  "private": true,
  "license": "MIT",
  "workspaces": ["shared", "server", "client"],
  "scripts": {
    "dev": "trap 'kill 0' INT; npm run dev -w server & npm run dev -w client & wait",
    "build": "npm run build -w server && npm run build -w client",
    "test": "npm run test -w server && npm run test -w client",
    "start": "npm run start -w server"
  },
  "engines": {
    "node": ">=18"
  }
}
